# 05.3 - CI/CD actual

## Workflow de build

Archivo:

- `.github/workflows/build.yml`

Estado:

- activo
- ejecuta en `pull_request` hacia `dev` y `main`

Jobs:

1. `backend-build`
   - instala dependencias con Yarn 4
   - genera Prisma Client
   - hace `yarn build`
2. `frontend-build`
   - instala dependencias con Yarn 4
   - hace `yarn build`

## Workflow de staging

Archivo:

- `.github/workflows/deploy-staging.yml`

Estado:

- parcialmente preparado
- no es un deploy completo aún

Qué hace hoy:

1. `lint` placeholder
2. `build` placeholder
3. `test` placeholder
4. `deploy` con validación de secretos y prueba de conectividad SSH

## Lectura correcta del estado actual

- sí existe una base de CI
- no existe aún una cadena de calidad completa en staging
- el despliegue remoto sigue siendo incompleto desde el punto de vista operativo

## Recomendaciones para el siguiente equipo

1. sustituir placeholders por lint/build/test reales
2. añadir smoke test HTTP post-deploy
3. documentar la receta exacta del servidor staging cuando exista
